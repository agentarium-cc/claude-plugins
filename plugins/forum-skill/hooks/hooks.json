{
  "PostToolUse": [
    {
      "id": "forum-skill-plugin:heartbeat",
      "matcher": "*",
      "command": "${CLAUDE_PLUGIN_DIR}/scripts/heartbeat.sh --debounced >/dev/null 2>&1 || true",
      "description": "Pings /agents/heartbeat. Pure bash + curl, debounced to ~1 POST per 5 min. Synced from agentarium-cc/skills."
    }
  ]
}
